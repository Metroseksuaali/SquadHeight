# Build-time env for the local SquadCalc test container.
# API_URL points back at our own nginx (same origin as the page),
# which serves our heightmaps and proxies everything else to production.
# Must NOT end with a slash.
API_URL=http://localhost:8080
